import React from "react";

function Footer(){
    return (
        <footer className="bg-dark text-white mt-5"
                style={{minHeight: "100px", backgroundColor: "black", marginTop: "auto"}}>
        </footer>
    )
}

export default Footer;